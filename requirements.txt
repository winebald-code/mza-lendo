# ─────────────────────────────────────────────────────────────────────────────
#  Mzalendo — core runtime dependencies
#  Python 3.11 → 3.13. Every package here resolves to a prebuilt wheel on
#  macOS, Linux and Windows, so `pip install -r requirements.txt` never needs
#  a compiler or any system library.
#
#  This is all you need to run the app: it defaults to SQLite.
#  For PostgreSQL, install requirements-postgres.txt instead.
# ─────────────────────────────────────────────────────────────────────────────

Flask==3.0.3
Flask-SQLAlchemy==3.1.1
Flask-Login==0.6.3
Flask-WTF==1.2.1
Flask-Limiter==3.8.0
SQLAlchemy==2.0.36
Werkzeug==3.0.4
python-dotenv==1.0.1
gunicorn==23.0.0
